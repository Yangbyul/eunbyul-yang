# eunbyul-webpage — 개인 홈페이지 (GitHub Pages)

> **워크스페이스 공통 계약.** 전체 작업 지도와 공통 규칙은 `~/research-agent/WORKSPACE.md`.

## 이 저장소는

양은별 교수의 개인 홈페이지. `index.html` + `data/`(업적 JSON) + `cv/` 구조이며 GitHub Pages로 공개된다.
**논문·발표 실적 반영은 research-agent의 `/실적` 스킬이 담당한다** — 그 런북(`~/.claude` memory의 project_personal_homepage)을 따르고, 여기서 임의로 데이터 구조를 바꾸지 않는다.

## 시작 절차

1. `git pull` 을 먼저 실행한다.

## 세션 시작 위치

이 저장소에는 프로젝트 에이전트·훅이 없다. 어디서 세션을 열어도 무방하다.
다만 실적 반영 작업은 `lab`(research-agent)에서 `/실적`으로 하는 것이 표준 경로다.

## 커밋 규칙

- 작업 완료 시마다 커밋 + 푸시(= 배포). 메시지는 한국어로 간결하게.
- push가 곧 공개 배포임을 기억한다 — 확정되지 않은 실적(게재확정 전 논문 등)을 올리지 않는다.
